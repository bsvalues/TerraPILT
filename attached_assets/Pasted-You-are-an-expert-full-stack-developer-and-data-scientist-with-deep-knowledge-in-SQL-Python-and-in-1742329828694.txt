You are an expert full-stack developer and data scientist with deep knowledge in SQL, Python, and interactive dashboard development using Streamlit and BI tools. Your task is to design, build, and optimize an end-to-end application for calculating and reporting Payment in Lieu of Tax (PILT) data for Benton County. The system must integrate data from a relational SQL database and existing Excel workbooks, incorporate complex calculation logic (including deductions, adjustments, and historical comparisons), and provide a highly interactive, user-friendly dashboard.

Your responsibilities include:
1. **Data Integration & Management:**
   - Connect to a SQL database using SQLAlchemy (or pyodbc if needed) to serve as the single source of truth for all PILT data.
   - Consolidate data from multiple tables (e.g., property_val, prop_supp_assoc, property, land_detail, property_exemption, tax_area, situs) and ensure data integrity.
   - Use Common Table Expressions (CTEs) and variables to centralize key constants (e.g., current assessment year, previous year) to avoid redundancy.

2. **Calculation Engine:**
   - Replicate and enhance the complex PILT calculation logic currently in Excel. This includes computing PILT dues per district based on assessed values, levy rates (per $1,000), and applying any necessary deductions (e.g., PL 81-874 adjustments).
   - Incorporate additional logic for historical comparisons and “what-if” scenarios.
   - Ensure that the calculation engine is modular, testable, and scalable.

3. **Interactive Dashboard:**
   - Develop an interactive web-based dashboard using Streamlit that displays:
     - Raw data (loaded from either Excel or the SQL database).
     - Calculated PILT amounts by district and aggregated totals.
     - Year-over-year trend analysis visualized with interactive charts (e.g., using Plotly).
     - “What-if” scenario sliders allowing real-time adjustment of parameters like levy rates, assessed values, and deduction amounts.
   - Provide options for detailed filtering (by district, year, land category, etc.) and enable export of reports to Excel or PDF.

4. **Application Architecture:**
   - Organize your project into clear modules (e.g., database.py for SQL integration, calculations.py for business logic, main.py for the Streamlit dashboard).
   - Secure database credentials and other sensitive configuration using environment variables or Replit secrets.
   - Write clean, well-documented, and maintainable code with inline comments, proper error handling, and testing hooks.

5. **User Feedback & Iteration:**
   - Build the initial prototype and prepare it for internal user testing. Gather feedback on data accuracy, usability, and desired additional features.
   - Iterate on the design based on feedback, improving interactivity, expanding report details, and adding scheduled report generation if required.

6. **Final Deliverable:**
   - A fully functional, interactive, and dynamic PILT application that replaces the manual Excel-based process with automated, real-time data calculations and visualizations.
   - The codebase should be deployable on Replit and easy to integrate with existing county systems.
   - Provide clear documentation for further maintenance and enhancement.

When responding, include detailed code samples (in Python/SQL), a clear project structure outline, and specific instructions for deployment and testing. Your goal is to provide a complete solution that demonstrates best practices in data integration, application development, and interactive reporting.

---

Follow this master prompt to generate a complete solution that meets the requirements described above.
